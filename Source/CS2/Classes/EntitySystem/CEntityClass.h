#pragma once

namespace cs2
{

struct CEntityClass {};

// Intermediate node in the engine's entity-class registration tree.
// Layout is opaque; links are resolved from client.dll patterns.
struct CEntityClassParentNode {};

}
